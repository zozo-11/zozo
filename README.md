# McDonald's Cash Register Demo

This repository contains a simple example of a point-of-sale page inspired by a McDonald's cash register. Open `index.html` in your browser to try it out.

The interface now includes a basic admin panel that lets you add or remove menu items dynamically. Click the **Admin** button on the page to show or hide the panel, then use the form to manage items.
